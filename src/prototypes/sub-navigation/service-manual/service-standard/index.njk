---
title: Service standard
description: Our core principles, standards and best practice lay out the path to building services for all users
version: 45.2.1
layout: sub-navigation/views/_master
hideSubnav: true
sortOrder: 1
---
{% block pageContent %}

    {% block main %}

        {{
            onsHero({
                "noBg": true,
                "wide": true,
                "title": pageInfo.title,
                "text": pageInfo.description
            })
        }}

        {% from "components/card/_macro.njk" import onsCard %}
        <div class="ons-container ons-container--wide">

            <div class="ons-grid ons-grid--column@xxs@s ons-u-mt-l">
                {% set cards = helpers.cardsNavigationHelper({ 
                    "pages": pageInfo.children
                }) %}
                {% for card in cards %}
                    <div class="ons-grid__col ons-col-4@m ons-u-mb-m@m">
                        {{ onsCard({
                            "id": 'title' + loop.index,
                            "textId": 'text-' + loop.index,
                            "title": card.title,
                            "url": card.url,
                            "text": card.description
                        }) }}
                    </div>
                {% endfor %}
            </div>
            
        </div>

    {% endblock %}

{% endblock %}
