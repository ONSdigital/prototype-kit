{
  "name": "{{PROJECT_NAME}}",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "license": "MIT",
  "author": {
    "name": "ONS Digital"
  },
  "scripts": {
    "start": "gulp prototype-kit:start",
    "watch": "gulp prototype-kit:watch",
    "build": "yarn tidy-clean && NODE_ENV=production gulp prototype-kit:build",
    "tidy-clean": "rm -rf build"
  },
  "dependencies": {
    "@ons/prototype-kit": "git+https://github.com/ONSdigital/prototype-kit#refactor-as-dependency",
    "gulp": "^4.0.2"
  }
}
